{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "application.serviceAccountName" . }}
  labels:
    {{- include "application.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.serviceAccount.syncWave | quote }}
automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
{{- end }}
{{- if and .Values.externalSecrets.enabled .Values.externalSecrets.serviceAccount.create }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "application.externalSecretsServiceAccountName" . }}
  labels:
    {{- include "application.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.externalSecrets.serviceAccount.syncWave | quote }}
automountServiceAccountToken: {{ .Values.externalSecrets.serviceAccount.automountServiceAccountToken }}
{{- end }}
